{
  "name": "2nth-skills",
  "version": "1.0.0",
  "private": true,
  "description": "2nth.ai Skills — AI agent skills catalog",
  "scripts": {
    "dev": "npx wrangler pages dev ./public",
    "deploy": "npx wrangler pages deploy ./public --project-name=2nth-skills --branch=main"
  }
}
